# План последующих действий (по итогам E2E)

Дата: 2026-03-22

## 1. Итоги последнего прогона

- Всего тестов: 33
- Успешно: 31
- Провалено: 2

Провалы только в WebKit (iPhone):
- `Homepage › should submit text and show success modal`
- `Интеграция с базой данных › создание текстового поздравления и открытие по ссылке`

## 2. Что уже сделано для iOS-совместимости

- Для текстовых поздравлений добавлен JSON POST (без FormData).
- API теперь принимает `application/json`.
- Введён fallback на `textarea` вместо rich-editor в iOS.
- Таб-контент управляется локально (без Base UI Tab Panels).
- Отправка формы привязана к нативному submit + requestSubmit + native click/touch обработчики.

## 3. Текущий риск

WebKit в Playwright не отправляет POST даже после нативных обработчиков. Это похоже на ограничение Playwright WebKit. Нужно проверить на реальном iOS устройстве.

## 4. Следующие шаги

1. Проверить отправку текста на реальном iPhone (Safari):
   - заполнить имя
   - ввести текст
   - отправить
   - убедиться, что появляется модал и создаётся запись
2. Если на реальном iPhone проблема сохраняется:
   - добавить iOS fallback отправки через `XMLHttpRequest` (без fetch)
   - включить логирование ошибок в продакшн-режиме для iOS
3. Для автотестов WebKit:
   - запускать headful, либо вынести WebKit в отдельный job/Cloud device farm.
